#import a module
import math
import random
import statistics
import keyword

#using module, run function
math.pow(2,3)
random.randint(0,100)

numbers = [1,5,33,12,46,33,2]
statistics.mean(numbers)
statistics.median(numbers)
statistics.mode(numbers)

keyword.iskeyword("for")
keyword.iskeyword('football')
